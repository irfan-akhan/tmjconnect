// /reports and /reports/:reportId both render the same split-pane inbox.
// The detail panel is driven by useParams() inside ReportsInboxPage.
export { ReportsInboxPage as ReportDetailPage } from './ReportsInboxPage';
